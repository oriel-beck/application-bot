export enum ApplicationState {
  Pending = 'pending',
  Active = 'active',
  Accepted = 'accepted',
  Denied = 'denied',
}

export enum ApplicationStateResponses {
  Disabled = 'disabled',
  Enabled = 'enabled',
}
